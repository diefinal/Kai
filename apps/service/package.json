{
  "name": "@kai/service",
  "version": "0.0.1",
  "scripts": {
    "build": "tsc -b",
    "lint": "eslint \"src/**/*.ts\"",
    "typecheck": "tsc --noEmit",
      "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@kai/shared": "workspace:*"
  }
}